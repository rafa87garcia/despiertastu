/**
 * @file
 * Behaviors for the despiertastu_varbase theme.
 */

(function($, _, Drupal) {
  Drupal.behaviors.despiertastu_varbase = {
    attach: function() {
      // despiertastu_varbase JavaScript behaviors goes here.
    }
  };
})(window.jQuery, window._, window.Drupal);
